@using Softsige.Ideias.App.ViewModels;

@model IEnumerable<ClaimViewModel>

@if (Model != null)
{
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nome do módulo</th>
                    <th>Permissões</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Type)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Value)
                            </td>
                            <td class="text-right">
                                <div class="btn-group">
                                    <a class="btn-white btn btn-xs" data-toggle="modal" href="#modal-excluir-claim" data-id="@ViewBag.UserId" data-claimValues="@item.Type;@item.Value">Excluir</a>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3">Nenhuma permissão adicionada até o momento</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}